#include <string.h>
#include "main.h"

/**
 * _strlen - returns the lenght of a string
 *
 * @count: count the caracters of the string
 */
int _strlen(char *s)
{
	int count;
	{
		count = strlen(s);
	}
	return (count);
}

#include <string.h>
#include "main.h"

/**
 * _strlen - returns the lenght of a string
 *
 * @s: string
 *
 * Return: size of the string
 */
int _strlen(char *s)
{
	int count;
	{
		count = strlen(s);
	}
	return (count);
}
